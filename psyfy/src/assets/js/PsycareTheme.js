import { createMuiTheme } from '@material-ui/core/styles';

const PsycareTheme = createMuiTheme({
    palette: {
        primary: {
            main: "#e57373"
        },
        secondary: {
            main: '#7986cb',
        },
    },
});

export default PsycareTheme;
